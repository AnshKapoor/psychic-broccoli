input:
  preprocessed_csv: "output/run/csv/preprocessed_OPTICS_exp_5.csv"

flows:
  flow_keys: []
  include: []

clustering:
  method: kmeans
  distance_metric: euclidean
  kmeans:
    n_clusters: 6
    n_init: "auto"
  evaluation:
    enabled: true
    include_noise: false

output:
  dir: output
  experiment_name: GLOBAL_KMEANS_exp_1

